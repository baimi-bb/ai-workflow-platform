# 2026-03-26 开发日志

## 今日完成

### 1. 检查并确认数据库连接方式
- 检查了后端数据库配置来源，确认应用从 `backend/.env` 读取 `BACKEND_DATABASE_URL`。
- 确认项目数据库设计为通过 `docker-compose.yml` 启动 PostgreSQL。
- 排查过一次数据库未启动的问题，定位到当时是 `localhost:5432` 没有监听。
- 在数据库启动后重新验证，确认后端已经可以成功连接 PostgreSQL。

### 2. 为三张业务表补齐 SQLAlchemy 模型
- 新增了 `Workspace`、`Project`、`Task` 三个模型文件：
  - `backend/app/models/workspace.py`
  - `backend/app/models/project.py`
  - `backend/app/models/task.py`
- 建立了模型关系：
  - `Workspace -> projects`
  - `Project -> workspace`
  - `Project -> tasks`
  - `Task -> project`
- 将主键和外键类型对齐为 `BIGINT`，与数据库中的 `BIGSERIAL/BIGINT` 结构保持一致。
- 更新了 `backend/app/models/__init__.py`，统一导出三个模型。

### 3. 新增创建和层级查询所需的 Pydantic Schema
- 最初将 `workspace/project/task` 相关 schema 集中在一个文件中实现。
- 后续根据项目结构需要，拆分为三个独立文件：
  - `backend/app/schemas/workspace.py`
  - `backend/app/schemas/project.py`
  - `backend/app/schemas/task.py`
- 补充了以下 schema：
  - `WorkspaceCreate`
  - `WorkspaceRead`
  - `WorkspaceWithProjects`
  - `ProjectCreate`
  - `ProjectRead`
  - `ProjectWithTasks`
  - `TaskCreate`
  - `TaskRead`
  - `TaskStatusUpdate`
- 更新了 `backend/app/schemas/__init__.py`，统一导出 schema。

### 4. 新增资源接口，打通 workspace -> project -> task 链路
- 在 `backend/app/api/routes/workspaces.py` 中新增并实现以下接口：
  - `POST /api/v1/workspaces/`
  - `GET /api/v1/workspaces/`
  - `GET /api/v1/workspaces/{workspace_id}`
  - `POST /api/v1/workspaces/{workspace_id}/projects`
  - `GET /api/v1/workspaces/{workspace_id}/projects`
  - `POST /api/v1/workspaces/{workspace_id}/projects/{project_id}/tasks`
  - `GET /api/v1/workspaces/{workspace_id}/projects/{project_id}/tasks`
  - `PATCH /api/v1/workspaces/{workspace_id}/projects/{project_id}/tasks/{task_id}/status`
- 在 `backend/app/api/router.py` 中注册了 `workspaces` 路由。

### 5. 在后端中增加父子关系校验
- 创建 `project` 时，先检查 `workspace` 是否存在。
- 创建 `task` 时，先检查 `project` 是否存在，并校验该 `project` 是否属于指定 `workspace`。
- 查询某个 `project` 下的 `tasks` 时，同样校验 `project` 是否属于指定 `workspace`。
- 更新 `task` 状态时，校验 `workspace -> project -> task` 这条链路是否一致，避免跨层级误操作。

### 6. 完成真实数据库验证
- 验证了数据库连接函数可以成功返回 `True`。
- 验证了数据库中已存在以下三张表：
  - `workspaces`
  - `projects`
  - `tasks`
- 通过真实数据库会话验证了以下流程可以跑通：
  - 创建 `workspace`
  - 在该 `workspace` 下创建 `project`
  - 在该 `project` 下创建 `task`
  - 查询某个 `workspace` 下的 `projects`
  - 查询某个 `project` 下的 `tasks`
  - 更新 `task.status`
- 验证了 FastAPI 路由成功加载，新接口已出现在应用路由列表中。

## 当前后端已具备的能力
- 创建 workspace
- 在 workspace 下创建 project
- 在 project 下创建 task
- 查询单个 workspace 的完整层级结构
- 查询某个 workspace 下的项目列表
- 查询某个 project 下的任务列表
- 更新 task 状态

## 当前说明
- 后端当前已经具备最小可用的数据创建与层级查询能力。
- 目前还没有补充 `workspace/project/task` 的完整更新与删除接口。
- 目前也还没有加入自动化测试文件，主要完成的是模型、schema、路由和数据库联调验证。

## 涉及文件
- `backend/app/api/router.py`
- `backend/app/api/routes/workspaces.py`
- `backend/app/models/__init__.py`
- `backend/app/models/workspace.py`
- `backend/app/models/project.py`
- `backend/app/models/task.py`
- `backend/app/schemas/__init__.py`
- `backend/app/schemas/workspace.py`
- `backend/app/schemas/project.py`
- `backend/app/schemas/task.py`

## 本次追加

### 7. 补齐 workspace / project / task 的更新与删除接口
- 新增了 `workspace` 更新接口：
  - `PATCH /api/v1/workspaces/{workspace_id}`
- 新增了 `workspace` 删除接口：
  - `DELETE /api/v1/workspaces/{workspace_id}`
- 新增了 `project` 更新接口：
  - `PATCH /api/v1/workspaces/{workspace_id}/projects/{project_id}`
- 新增了 `project` 删除接口：
  - `DELETE /api/v1/workspaces/{workspace_id}/projects/{project_id}`
- 新增了 `task` 通用更新接口：
  - `PATCH /api/v1/workspaces/{workspace_id}/projects/{project_id}/tasks/{task_id}`
- 新增了 `task` 删除接口：
  - `DELETE /api/v1/workspaces/{workspace_id}/projects/{project_id}/tasks/{task_id}`

### 8. 为更新接口补充 Update Schema
- 在 `backend/app/schemas/workspace.py` 中新增 `WorkspaceUpdate`
- 在 `backend/app/schemas/project.py` 中新增 `ProjectUpdate`
- 在 `backend/app/schemas/task.py` 中新增 `TaskUpdate`
- 更新了 `backend/app/schemas/__init__.py`，导出新增的 update schema

### 9. 抽取层级校验辅助函数
- 在 `backend/app/api/routes/workspaces.py` 中新增了以下辅助函数，用于复用层级校验逻辑：
  - `_get_workspace_or_404`
  - `_get_project_for_workspace_or_404`
  - `_get_task_for_project_or_404`
- 这样可以避免在多个接口里重复书写相同的存在性与归属关系校验。

### 10. 完成更新/删除能力验证
- 验证了 `workspace` 字段更新后可以正常持久化
- 验证了 `project` 名称、描述、状态更新后可以正常持久化
- 验证了 `task` 标题、描述、状态、优先级更新后可以正常持久化
- 验证了 `task` 删除成功
- 验证了 `project` 删除成功
- 验证了 `workspace` 删除成功

## 当前后端能力补充
- 更新 workspace
- 删除 workspace
- 更新 project
- 删除 project
- 更新 task 全量业务字段
- 删除 task

## 本次追加

### 11. 搭建前端三层测试页面流程
- 将前端首页从健康检查展示页升级为 workspace 测试主页
- 首页现在可以：
  - 拉取并展示已有 workspace 列表
  - 创建新的 workspace
  - 点击进入指定 workspace 详情页
- 新增 workspace 详情页：
  - 路径：`/workspaces/[workspaceId]`
  - 展示当前 workspace 的基本信息
  - 展示该 workspace 下已有的 project 列表
  - 支持创建新的 project
  - 保留返回 workspace 首页入口
- 新增 project 详情页：
  - 路径：`/workspaces/[workspaceId]/projects/[projectId]`
  - 展示当前 project 的基本信息
  - 展示该 project 下已有的 task 列表
  - 支持创建新的 task
  - 支持修改 task 状态
  - 保留返回 workspace 首页和 workspace 详情页入口

### 12. 抽离前端公共请求与服务端操作
- 新增 `frontend/lib/api.ts`
  - 统一封装 workspaces / projects / tasks / health 的请求
  - 统一处理 `404 -> notFound()`
- 新增 `frontend/lib/actions.ts`
  - 封装创建 workspace 的 server action
  - 封装创建 project 的 server action
  - 封装创建 task 的 server action
  - 封装更新 task 状态的 server action
  - 在提交后自动 `revalidatePath`
- 新增 `frontend/lib/types.ts`
  - 统一定义 `Workspace / Project / Task` 前端类型
- 新增 `frontend/lib/ui.ts`
  - 提供日期格式化函数
  - 提供状态与优先级标签样式映射

### 13. 调整前端页面文案与导航结构
- 更新 `frontend/app/layout.tsx`
  - 页面语言调整为 `zh-CN`
  - 页面描述改为前端测试流程说明
- 首页、workspace 页、project 页均加入清晰的返回入口
- 页面布局围绕“测试业务流”设计，而不是保留默认脚手架首页

### 14. 完成前端代码检查
- 运行 `npm.cmd run lint`
- lint 已通过，当前新增前端页面与公共逻辑无 ESLint 报错

## 本次涉及文件
- `frontend/app/page.tsx`
- `frontend/app/workspaces/[workspaceId]/page.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/page.tsx`
- `frontend/app/layout.tsx`
- `frontend/lib/api.ts`
- `frontend/lib/actions.ts`
- `frontend/lib/types.ts`
- `frontend/lib/ui.ts`

## 本次追加

### 15. 补充前端编辑与删除入口
- 为 workspace 补充编辑与删除能力
  - 在 workspace 详情页新增编辑表单
  - 在 workspace 详情页新增删除按钮
- 为 project 补充编辑与删除能力
  - 在 workspace 详情页的 project 卡片中新增编辑表单
  - 在 workspace 详情页的 project 卡片中新增删除按钮
  - 在 project 详情页左侧新增 project 编辑表单与删除按钮
- 为 task 补充编辑与删除能力
  - 在 project 详情页的 task 卡片中新增完整编辑表单
  - 保留原有的 task 状态快速更新入口
  - 在 task 卡片中新增删除按钮

### 16. 扩展前端公共 API 与 Server Action
- 在 `frontend/lib/api.ts` 中新增：
  - `updateWorkspace`
  - `deleteWorkspace`
  - `updateProject`
  - `deleteProject`
  - `updateTask`
  - `deleteTask`
- 在 `frontend/lib/actions.ts` 中新增：
  - `updateWorkspaceAction`
  - `deleteWorkspaceAction`
  - `updateProjectAction`
  - `deleteProjectAction`
  - `updateTaskAction`
  - `deleteTaskAction`
- 删除后按层级进行页面跳转或刷新
- 编辑后通过 `revalidatePath` 刷新相关页面数据

### 17. 完成前端代码检查
- 再次运行 `npm.cmd run lint`
- lint 已通过，当前编辑/删除功能新增代码无 ESLint 报错

## 本次涉及文件补充
- `frontend/app/workspaces/[workspaceId]/page.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/page.tsx`

## 本次追加

### 21. 细化对象操作菜单交互
- 将 `...` 菜单调整为更小的对象操作栏
- 点击 `...` 后只展示较小的“编辑 / 删除”入口
- 点击“编辑”后再展开编辑表单，并通过“确认编辑”提交
- 点击“删除”后先展开确认区，再通过“确认删除”执行删除

### 22. 补齐 workspace 列表页的对象操作
- 在首页每个 workspace 卡片上新增 `...` 操作按钮
- 支持在首页直接编辑 workspace
- 支持在首页直接删除 workspace
- 同时保留进入 workspace 详情页的入口

### 23. 调整 task 操作区位置
- 将 task 状态更新从 `...` 菜单中移出
- task 卡片外层保留独立的状态更新表单
- `...` 菜单只保留 task 的编辑和删除操作

### 24. 清理页面文案与结构
- 重写首页、workspace 详情页、project 详情页相关页面结构
- 同时修正此前页面中出现的部分乱码文案

### 25. 完成前端代码检查
- 再次运行 `npm.cmd run lint`
- lint 已通过，当前交互调整无 ESLint 报错

## 本次涉及文件补充
- `frontend/app/page.tsx`
- `frontend/app/workspaces/[workspaceId]/page.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/page.tsx`

## 本次追加

### 26. 统一后端异常返回格式
- 新增全局异常响应结构：
  - `code`
  - `message`
  - `details`
- 新增全局异常处理器，统一处理以下场景：
  - `HTTPException`
  - `RequestValidationError`
  - 未处理的通用异常
- 统一后，接口报错时前端可以按固定结构读取错误信息，而不必分别兼容多种返回格式

### 27. 补充输入校验
- 为 `workspace` 输入补充约束：
  - `name` 去除首尾空格
  - `name` 必填且不能为空白
  - `name` 最大长度限制
  - `description` 为空字符串时归一化为 `None`
  - `description` 最大长度限制
- 为 `project` 输入补充约束：
  - `name` 去除首尾空格
  - `name` 必填且不能为空白
  - `status` 限定为允许值：`active / paused / done`
  - `description` 为空字符串时归一化为 `None`
  - `description` 最大长度限制
- 为 `task` 输入补充约束：
  - `title` 去除首尾空格
  - `title` 必填且不能为空白
  - `status` 限定为允许值：`todo / in_progress / done`
  - `priority` 限定为允许值：`low / medium / high`
  - `description` 为空字符串时归一化为 `None`
  - `description` 最大长度限制

### 28. 调整后端应用启动异常链
- 在 FastAPI 应用入口中注册全局异常处理器
- 让接口校验失败和业务异常都通过统一的错误响应层返回

### 29. 本轮说明
- 本轮先完成：
  - 统一异常返回格式
  - 输入校验
- `Alembic migration` 本轮尚未开始，准备放到下一轮单独处理
- 当前终端环境中的 `python` 与 `py` 命令不可用，因此未能执行运行时导入验证

## 本次涉及文件补充
- `backend/app/main.py`
- `backend/app/api/errors.py`
- `backend/app/schemas/common.py`
- `backend/app/schemas/error.py`
- `backend/app/schemas/workspace.py`
- `backend/app/schemas/project.py`
- `backend/app/schemas/task.py`
- `backend/app/schemas/__init__.py`

## 本次追加

### 30. 接入 Alembic migration 基础设施
- 在后端新增 Alembic 配置文件：
  - `backend/alembic.ini`
  - `backend/alembic/env.py`
  - `backend/alembic/script.py.mako`
- 在 `env.py` 中接入项目已有的：
  - `settings.database_url`
  - `SQLAlchemy Base.metadata`
  - 当前 `Workspace / Project / Task` 模型
- 让 Alembic 后续可以直接基于现有模型做迁移管理

### 31. 新增初始基线迁移
- 新增首个 migration：
  - `backend/alembic/versions/20260326_0001_initial_schema.py`
- 该版本定义了以下三张表的初始结构：
  - `workspaces`
  - `projects`
  - `tasks`
- 同时补充了：
  - 外键约束
  - 级联删除
  - 关联字段索引

### 32. 更新后端依赖与文档
- 在 `backend/requirements.txt` 中新增 `alembic`
- 在 `backend/README.md` 中补充迁移命令说明：
  - `alembic upgrade head`
  - `alembic revision --autogenerate`
  - `alembic downgrade -1`
- 同时修正 `BACKEND_DATABASE_URL` 示例

### 33. 完成 Alembic 实际验证
- 使用 `backend/venv` 安装新增依赖
- 成功验证：
  - `alembic history`
  - `alembic current`
- 执行 `alembic upgrade head` 时发现当前数据库中业务表已存在
- 因此采用基线接管方式执行：
  - `alembic stamp head`
- 当前数据库已成功标记为：
  - `20260326_0001 (head)`

### 34. 本轮说明
- 这次没有删除现有表，也没有重建数据库
- 当前做法是把已有数据库纳入 Alembic 版本管理
- 后续如再修改模型，建议开始使用：
  - `alembic revision --autogenerate -m "..."` 生成新迁移
  - `alembic upgrade head` 应用新迁移

## 本次涉及文件补充
- `backend/requirements.txt`
- `backend/README.md`
- `backend/alembic.ini`
- `backend/alembic/env.py`
- `backend/alembic/script.py.mako`
- `backend/alembic/versions/20260326_0001_initial_schema.py`

## 本次追加

### 35. 整理 workspace / project / task 业务逻辑分层
- 将原本集中写在路由文件中的业务逻辑下沉到服务层
- 新增：
  - `backend/app/services/__init__.py`
  - `backend/app/services/workspace_service.py`
- 路由层保留：
  - HTTP 路径定义
  - 请求参数接收
  - 响应模型声明
- 服务层负责：
  - 查询与层级校验
  - 创建 / 更新 / 删除
  - 数据库提交与刷新

### 36. 下沉的主要逻辑
- `create_workspace`
- `create_project_under_workspace`
- `create_task_under_project`
- `update_task_status`
- 以及同一文件中的其他 workspace / project / task CRUD 与列表查询逻辑
- 原先的辅助校验函数也一起迁移到 service 中：
  - `get_workspace_or_404`
  - `get_project_for_workspace_or_404`
  - `get_task_for_project_or_404`

### 37. 路由文件瘦身
- 重写 `backend/app/api/routes/workspaces.py`
- 现在路由函数主要改为调用 `workspace_service` 中的函数
- 让后续继续增加 service、测试、权限控制时结构更清晰

### 38. 完成后端导入验证
- 使用 `backend/venv` 进行最小导入检查
- 成功验证：
  - `workspace_service` 可导入
  - 路由对象可导入
  - FastAPI 应用可正常导入

## 本次涉及文件补充
- `backend/app/api/routes/workspaces.py`
- `backend/app/services/__init__.py`
- `backend/app/services/workspace_service.py`

## 本次追加

### 39. 将大 service 继续拆成领域 service
- 原本集中在 `workspace_service.py` 中的逻辑继续按领域拆分为：
  - `backend/app/services/workspace_service.py`
  - `backend/app/services/project_service.py`
  - `backend/app/services/task_service.py`
- 目标是让 workspace / project / task 的业务逻辑各自归位，减少单文件持续膨胀

### 40. 新的职责划分
- `workspace_service.py`
  - `create_workspace`
  - `update_workspace`
  - `delete_workspace`
  - `list_workspaces`
  - `get_workspace_or_404`
  - `get_workspace_with_projects_or_404`
- `project_service.py`
  - `create_project_under_workspace`
  - `update_project`
  - `delete_project`
  - `list_projects_for_workspace`
  - `get_project_for_workspace_or_404`
- `task_service.py`
  - `create_task_under_project`
  - `update_task_status`
  - `update_task`
  - `delete_task`
  - `list_tasks_for_project`
  - `get_task_for_project_or_404`

### 41. 调整服务导出与路由引用
- 更新 `backend/app/services/__init__.py`
- 调整 `backend/app/api/routes/workspaces.py`
- 现在路由层分别调用：
  - `workspace_service`
  - `project_service`
  - `task_service`

### 42. 完成后端导入验证
- 验证三个 service 模块均可正常导入
- 验证路由对象可正常导入
- 验证 FastAPI 应用可正常导入

## 本次涉及文件补充
- `backend/app/services/workspace_service.py`
- `backend/app/services/project_service.py`
- `backend/app/services/task_service.py`
- `backend/app/services/__init__.py`
- `backend/app/api/routes/workspaces.py`

## 本次追加

### 43. 补充 service 层单元测试
- 新增 `backend/tests/` 测试目录
- 新增 service 层测试文件：
  - `backend/tests/services/test_workspace_service.py`
  - `backend/tests/services/test_project_service.py`
  - `backend/tests/services/test_task_service.py`
- 本轮测试采用：
  - `unittest`
  - `unittest.mock`
- 先覆盖 service 逻辑本身，不依赖独立测试数据库

### 44. 当前已覆盖的 service 逻辑
- `workspace_service`
  - 创建 workspace
  - 更新 workspace
  - workspace 不存在时抛出 404
- `project_service`
  - 在 workspace 下创建 project
  - 更新 project
  - project 归属错误时抛出 400
- `task_service`
  - 在 project 下创建 task
  - 更新 task
  - 更新 task 状态
  - task 归属错误时抛出 400

### 45. 完成测试执行验证
- 使用 `backend/venv` 运行：
  - `python -m unittest discover -s tests -p "test_*.py" -v`
- 结果：
  - 共运行 10 个测试
  - 全部通过

### 46. 本轮说明
- 这轮先建立 service 层的基础测试网
- 当前更偏单元测试，重点验证：
  - 字段更新逻辑
  - 提交与刷新调用
  - 层级归属校验
  - 关键异常抛出
- 如后续需要更强保障，可再补：
  - 基于测试数据库的集成测试
  - API 路由测试

## 本次涉及文件补充
- `backend/tests/__init__.py`
- `backend/tests/services/__init__.py`
- `backend/tests/services/test_workspace_service.py`
- `backend/tests/services/test_project_service.py`
- `backend/tests/services/test_task_service.py`
- `frontend/lib/api.ts`
- `frontend/lib/actions.ts`

## 本次追加

### 18. 调整编辑与删除入口的展示形式
- 将直接平铺在页面中的编辑表单和删除按钮，改为 `...` 操作菜单形式
- 点击 `...` 后再展开对应目标的操作区域，避免测试页在数据较多时显得过长
- 当前调整范围：
  - workspace 详情页中的 workspace 操作
  - workspace 详情页中的 project 卡片操作
  - project 详情页中的 project 操作
  - project 详情页中的 task 卡片操作

### 19. 优化测试页可读性
- 保留创建表单为常驻展示
- 将编辑与删除归类为对象级管理操作
- 继续保留返回入口与层级导航，避免页面折叠后影响流转测试

### 20. 完成前端代码检查
- 再次运行 `npm.cmd run lint`
- lint 已通过，当前菜单式编辑/删除交互无 ESLint 报错

## 本次涉及文件补充
- `frontend/app/workspaces/[workspaceId]/page.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/page.tsx`
