## 本次追加

### 1. 新增任务依赖字段 `task_dependencies`
- 后端任务模型新增 `task_dependencies: list[int]`
- 任务读写 schema 同步补充该字段
- 前端 `Task` 类型与 API 请求参数同步补充该字段

### 2. 增加任务依赖规则校验
- 允许表达“任务 B 依赖任务 A”
- 当依赖任务未完成时，当前任务不能进入：
  - `in_progress`
  - `done`
- 限制依赖必须属于同一个 project
- 限制任务不能依赖自己
- 限制任务依赖不能形成循环

### 3. 补充删除保护
- 当某任务仍被其他任务依赖时，禁止直接删除
- 返回 `409`

### 4. 前端补充依赖录入与展示
- 创建 task 时可填写 `task_dependencies`
- 编辑 task 时可修改 `task_dependencies`
- project 详情页 task 卡片展示：
  - 当前依赖任务 ID
  - 未完成依赖数量

### 5. 数据库迁移
- 新增 Alembic 迁移：
  - `backend/alembic/versions/20260327_0002_add_task_dependencies.py`

### 6. 验证
- 后端验证：
  - `python -m unittest backend.tests.services.test_task_service`
- 前端验证：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/models/task.py`
- `backend/app/schemas/task.py`
- `backend/app/services/task_service.py`
- `backend/tests/services/test_task_service.py`
- `backend/alembic/versions/20260327_0002_add_task_dependencies.py`
- `frontend/lib/types.ts`
- `frontend/lib/api.ts`
- `frontend/lib/actions.ts`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/page.tsx`

## 本次追加

### 7. 拆分 project 内部为两个子页面
- 原 `project` 页面改为默认跳转到：
  - `List / Table`
  - `Workflow`
- 保留原路径入口，避免旧链接失效

### 8. 新增 List / Table 视图
- 新增独立子路由：
  - `frontend/app/workspaces/[workspaceId]/projects/[projectId]/list/page.tsx`
- 支持按以下维度快速查看 task：
  - 名称
  - 状态
  - 负责人占位
  - 优先级
  - 创建时间
  - 更新时间
  - 依赖情况
- 增加基础筛选：
  - 关键词
  - 状态
  - 优先级
- 增加批量改状态能力

### 9. 新增 Workflow 视图
- 新增独立子路由：
  - `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/page.tsx`
- 根据 `task_dependencies` 自动计算层级
- 按执行阶段展示任务列
- 展示依赖关系和流转摘要

### 10. 抽离 project 共享页面骨架
- 新增共享文件：
  - `frontend/app/workspaces/[workspaceId]/projects/[projectId]/project-view.tsx`
- 抽离公共区域：
  - project 头部
  - 子页面切换入口
  - project 操作区
  - 创建 task 表单
  - task 状态/优先级/依赖展示组件

### 11. 前端联动调整
- 新增批量更新 task 状态的 server action
- task 创建、编辑、删除、状态更新后同步刷新：
  - 原 project 路径
  - list 子页面
  - workflow 子页面

### 12. 验证
- 执行：
  - `npm.cmd run lint`
- 结果：
  - lint 通过

## 本次涉及文件补充
- `frontend/lib/actions.ts`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/page.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/project-view.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/list/page.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/page.tsx`

## 本次追加

### 13. Workflow 画布升级为流程节点视图
- 在 Workflow 页面增加 SVG 箭头连线展示任务依赖关系
- 画布按阶段与顺序摆放节点
- 节点卡片展示：
  - 节点类型
  - 状态
  - 优先级
  - 顺序
  - 依赖信息

### 14. 支持在 Workflow 视图中管理流程
- 支持添加节点：
  - 继续沿用 project 侧边创建 task 表单
- 支持添加连线：
  - 新增起点/终点节点选择表单
- 支持调整顺序：
  - 为普通节点增加上移 / 下移操作
- 支持配置节点：
  - 可编辑标题
  - 可编辑描述
  - 可编辑状态
  - 可编辑优先级
  - 可编辑依赖节点

### 15. 引入 start 开始节点
- 新增任务字段：
  - `node_type`
  - `display_order`
- 每个 project 创建时自动生成一个 `start` 节点
- `start` 节点默认：
  - 标题为 `Start`
  - 状态为 `done`
  - 优先级为 `low`
  - 顺序为 `0`
- 限制：
  - `start` 节点不可删除
  - `start` 节点不可依赖其他节点

### 16. 数据库迁移与历史数据补齐
- 新增迁移：
  - `backend/alembic/versions/20260327_0003_add_task_node_fields.py`
- 迁移内容包括：
  - 为 task 增加 `node_type`
  - 为 task 增加 `display_order`
  - 为已有 task 回填顺序
  - 为历史 project 自动补齐 `start` 节点

### 17. 前后端联动调整
- 前端 Task 类型同步补充：
  - `node_type`
  - `display_order`
- 新增前端 action：
  - `connectTaskAction`
  - `moveTaskOrderAction`
- 更新 task 相关刷新路径，保证 list / workflow 同步更新

### 18. 验证
- 后端：
  - `python -m unittest backend.tests.services.test_project_service backend.tests.services.test_task_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/models/task.py`
- `backend/app/schemas/task.py`
- `backend/app/services/project_service.py`
- `backend/app/services/task_service.py`
- `backend/alembic/versions/20260327_0003_add_task_node_fields.py`
- `backend/tests/services/test_project_service.py`
- `backend/tests/services/test_task_service.py`
- `frontend/lib/types.ts`
- `frontend/lib/api.ts`
- `frontend/lib/actions.ts`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/project-view.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/page.tsx`

## 本次追加

### 19. Workflow 支持拖拽节点位置
- 新增 task 字段：
  - `canvas_x`
  - `canvas_y`
- 节点在 Workflow 画布中拖动后会持久化保存
- 刷新页面后仍保留上次布局

### 20. Workflow 支持直接拖线式连接
- 新增交互画布组件：
  - `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`
- 节点卡片支持：
  - 点击开始连线
  - 再点击目标节点完成连接
- 连线关系会直接写回 `task_dependencies`
- 画布中使用 SVG 箭头展示流程关系

### 21. 旧数据兼容与位置回填
- 新增迁移：
  - `backend/alembic/versions/20260327_0004_add_task_canvas_position.py`
- 迁移会为已有任务补充默认画布位置
- 历史 `start` 节点与普通节点都会回填到可展示位置

### 22. 前后端联动补充
- Task schema / model / 前端类型同步增加画布坐标字段
- task 创建时为新节点分配默认画布位置
- project 创建时自动生成的 `start` 节点也带默认坐标

### 23. 验证
- 后端：
  - `python -m unittest backend.tests.services.test_project_service backend.tests.services.test_task_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/models/task.py`
- `backend/app/schemas/task.py`
- `backend/app/services/project_service.py`
- `backend/app/services/task_service.py`
- `backend/alembic/versions/20260327_0004_add_task_canvas_position.py`
- `backend/tests/services/test_project_service.py`
- `backend/tests/services/test_task_service.py`
- `frontend/lib/types.ts`
- `frontend/lib/api.ts`
- `frontend/lib/actions.ts`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/page.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`

## 本次追加

### 24. 调整 Workflow 页面布局
- 将“添加连线”区域从右侧控制栏移动到左侧创建任务栏下方
- 去掉独立的“节点配置与顺序调整”右侧栏
- 让 Workflow 主画布区域获得更大的可用宽度

### 25. 将节点配置入口移动到画布节点上
- 每个节点卡片内新增按钮：
  - 配置节点
  - 开始连线
  - 上移
  - 下移
- 配置动作改为由节点内部按钮触发
- 配置面板保留在画布下方展开，不再占用右侧整列

### 26. 扩大 Workflow 画布并支持缩放
- 扩大节点尺寸与画布边距
- 增加画布缩放控制：
  - 放大
  - 缩小
- 画布容器支持更大的滚动展示区域

### 27. 验证
- 前端：
  - `npm.cmd run lint`
- 结果：
  - lint 通过

## 本次涉及文件补充
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/project-view.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/page.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`

## 本次追加

### 28. 精简节点按钮
- 去掉节点上的：
  - 上移
  - 下移
- 保留节点内核心入口：
  - 配置节点
  - 开始连线

### 29. 修复并重做连线交互
- 将连线交互改为真正的预览模式：
  - 选择起点后，鼠标移动时显示实时预览线
  - 点击目标节点后完成连线
- 优化连线状态切换，减少误触

### 30. 新增框选与多选拖动
- 支持在画布空白区域框选多个节点
- 被选中的节点可一起拖动
- 拖动结束后批量保存节点位置

### 31. 新增画布导航能力
- 支持 `Ctrl/Command + 滚轮` 缩放
- 支持按钮缩放
- 支持按住空格拖动画布

### 32. 支持右键删除连线
- 在画布上对箭头连线执行右键操作可直接删除该依赖关系

### 33. 验证
- 前端：
  - `npm.cmd run lint`
- 结果：
  - lint 通过

## 本次涉及文件补充
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/page.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`

## 本次追加

### 34. 修复 Workflow 画布中的客户端 PATCH 失败
- 问题表现：
  - 画布中删除连线/更新节点时浏览器报 `Failed to fetch`
- 原因：
  - Workflow 画布组件在浏览器侧直接请求后端 API 地址
  - 在当前运行环境下该方式不稳定，容易触发跨域或直连失败
- 修复方式：
  - 新增 Next 同源 API 代理路由
  - 画布内的节点更新请求改走前端自身 `/api/...` 路由再转发到后端

### 35. 验证
- 前端：
  - `npm.cmd run lint`
- 结果：
  - lint 通过

## 本次涉及文件补充
- `frontend/app/api/workspaces/[workspaceId]/projects/[projectId]/tasks/[taskId]/route.ts`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`

## 本次追加

### 36. 修复拖动节点后保存失败与位置回弹
- 问题表现：
  - 拖动节点后报 `Failed to update task`
  - 节点刷新后回到原位置
- 原因：
  - 画布保存位置时附带回写了过多 task 字段
  - 某些旧数据在一起提交时会触发后端校验，导致位置更新失败
- 修复方式：
  - 拖动节点时只提交：
    - `canvas_x`
    - `canvas_y`
  - 添加/删除连线时只提交：
    - `task_dependencies`
  - 保留节点配置表单在需要时单独提交完整编辑字段
- 额外优化：
  - `patchTask` 现在会抛出带后端响应内容的错误，便于继续定位问题

### 37. 验证
- 前端：
  - `npm.cmd run lint`
- 结果：
  - lint 通过

## 本次涉及文件补充
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`

## 本次追加

### 46. 调整节点配置面板为浮动抽屉
- 原问题：
  - 点击“配置节点”按钮后反馈不明显
- 调整后：
  - 改为右侧浮动抽屉
  - 打开时带遮罩和明显的层级变化
  - 节点配置状态更加直观

### 47. 画布节点状态样式补充
- 画布节点上的状态标签改为复用统一状态色
- 优先级标签也改为复用统一优先级色
- 节点卡片上的状态信息更接近 list/table 视图表现

### 48. 验证
- 前端：
  - `npm.cmd run lint`
- 结果：
  - lint 通过

## 本次涉及文件补充
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`

## 本次追加

### 42. 为每个 project 增加默认 end 结束节点
- 新建 project 时，系统现在会自动创建：
  - `start` 开始节点
  - `end` 结束节点
- `end` 节点默认：
  - 标题为 `End`
  - 节点类型为 `end`
  - 状态为 `todo`
  - 优先级为 `low`

### 43. 历史 project 补齐 end 节点
- 通过迁移为旧 project 自动补充缺失的 `end` 节点
- 同步为 `end` 节点补默认画布位置

### 44. 系统节点规则补充
- 前后端节点类型扩展为：
  - `start`
  - `task`
  - `end`
- 普通创建接口不允许手动创建 `end` 节点
- 后端不允许删除 `end` 节点

### 45. 验证
- 后端：
  - `python -m unittest backend.tests.services.test_project_service backend.tests.services.test_task_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/schemas/task.py`
- `backend/app/services/project_service.py`
- `backend/app/services/task_service.py`
- `backend/alembic/versions/20260327_0003_add_task_node_fields.py`
- `backend/alembic/versions/20260327_0004_add_task_canvas_position.py`
- `backend/tests/services/test_project_service.py`
- `backend/tests/services/test_task_service.py`
- `frontend/lib/types.ts`

## 本次追加

### 40. 修复画布内滚轮导致页面上下滚动
- 问题：
  - 鼠标位于画布内滚轮缩放时，外层页面仍然会跟着上下滚动
- 修复：
  - 在画布滚轮事件中增加 `stopPropagation`
  - 画布容器增加 `overscroll-none`
- 结果：
  - 滚轮在画布内优先作用于画布，不再带动页面上下滚动

### 41. 验证
- 前端：
  - `npm.cmd run lint`
- 结果：
  - lint 通过

## 本次涉及文件补充
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`

## 本次追加

### 38. 调整画布缩放手势
- 原逻辑：
  - 需要 `Ctrl/Command + 滚轮` 才会缩放画布
  - 实际使用时更容易触发浏览器/页面整体缩放
- 调整后：
  - 鼠标位于画布容器内时，直接滚轮即可缩放画布
  - 缩放手势优先作用于 Workflow 画布本身

### 39. 验证
- 前端：
  - `npm.cmd run lint`
- 结果：
  - lint 通过

## 本次涉及文件补充
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`

## 本次追加

### 49. 合并 List / Table 页面中的单任务操作
- 移除表格下方独立的“单任务操作 / Quick Manage”区域
- 在任务表格中新增“操作”列
- 每一行直接内嵌原有 `TaskRowActions`，避免同一批任务信息重复展示两遍
- 批量操作区保留在表格上方，单任务操作改为就地处理

### 50. 验证
- 前端：
  - `npm.cmd run lint`
- 结果：
  - lint 通过

## 本次涉及文件补充
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/list/page.tsx`

## 本次追加

### 51. 优化单任务状态更新失败反馈
- 任务存在未完成前置依赖时，尝试更新为 `in_progress` 或 `done` 不再直接抛出运行时错误
- 前端改为拒绝本次状态变更，并弹窗提示具体原因
- 状态更新失败后会把下拉选择恢复到原来的状态值
- API 错误信息读取补强，优先展示后端返回的 `detail`

### 52. 精简编辑栏并优化收起行为
- 去掉任务编辑表单中的“状态”字段，避免与单独的“更新状态”功能重复
- 点击“确认编辑”成功后，自动收起编辑栏与外层操作面板
- 单任务操作区域改为客户端组件，便于处理弹窗、关闭面板与交互反馈

### 53. 验证
- 前端：
  - `npm.cmd run lint`
- 结果：
  - lint 通过

## 本次涉及文件补充
- `frontend/lib/api.ts`
- `frontend/lib/actions.ts`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/project-view.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/task-row-actions-client.tsx`

## 本次追加

### 54. 修复 List / Table 页面中的嵌套表单问题
- 问题原因：批量更新状态的外层 `form` 包住了整张任务表格，而每个任务行里又包含自己的编辑 / 更新状态 / 删除表单
- 这会导致 React 在提交时识别到意外的表单提交，出现：
  - `A React form was unexpectedly submitted`
- 修复方式：
  - 将批量更新表单从表格外层拆出
  - 通过 `form` 属性把批量操作下拉、提交按钮、行勾选框关联到同一个批量表单
  - 让每个任务行内的单独表单恢复为合法的独立提交

### 55. 验证
- 前端：
  - `npm.cmd run lint`
- 结果：
  - lint 通过

## 本次涉及文件补充
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/list/page.tsx`
