# 2026-04-02

## 本次追加

### 1. AI 生成后的 workflow 布局进一步优化为分支更自然的流程图
- 在已有自动连线与默认依赖补全的基础上，继续优化了 AI 生成节点的初始布局
- 这次不再只是按层级简单从上往下堆叠，而是增加了按父节点重心排序的布局规则
- 目标是让多分支 workflow 默认更像一张可读的流程图

### 2. 新增按父节点重心的分层排版规则
- 任务节点仍然按照依赖深度从左到右分层
- 同一层中的节点现在会优先参考其前置依赖节点所在的纵向位置排序
- 因此同一个父节点下面的分支会更自然地聚在一起，减少视觉跳跃

### 3. Start / End 节点位置也会随分支中心对齐
- `start` 节点现在会根据首层任务节点的中线位置自动调整纵向位置
- `end` 节点会根据末层终点任务的中线位置自动调整纵向位置
- 这样在多分支场景下，入口和出口会更接近整条流程的视觉中心

### 4. 测试补充
- 更新 `backend/tests/services/test_project_planning_service.py`
- 新增覆盖：
  - 分支任务在不同层级下按父节点重心排序
  - 现有 AI 规划流程仍能正确补全依赖与布局

### 5. 验证
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_project_planning_service tests.services.test_workflow_run_service`
- 结果：
  - 单测通过

## 本次涉及文件
- `backend/app/services/project_planning_service.py`
- `backend/tests/services/test_project_planning_service.py`

## 本次追加

### 62. 新增 Task Run 运行活动日志，可看到“正在做什么”和“具体做了什么”
- 为了提升可观测性，在 `backend/app/services/agent_execution_service.py` 中新增了 task run 活动事件记录
- 现在 task run 执行过程中会持续记录关键阶段，例如：
  - `start`
  - `prepare`
  - `model_request`
  - `model_response`
  - `tool_request`
  - `tool_execute`
  - `tool_result`
  - `retry`
  - `result`
  - `completed`
  - `failed`
- 活动日志会写入 `task_run.input_payload.activity_log`，并在完成后同步保留到 `task_run.output_payload.activity_log`

### 63. 前端执行期间自动刷新，支持实时观察活动变化
- 在：
  - `execute-task-run-button.tsx`
  - `execute-workflow-run-button.tsx`
- 中新增了执行中的定时 `router.refresh()`
- 这样当后端持续写入活动日志时，页面会在执行过程中自动刷新，用户不需要等任务结束才能看到进展

### 64. Run Detail 和画布都可展示当前活动
- `workflow/page.tsx` 的 Run Detail 现在新增 `Activity` 面板
- 会展示每个 task run 的活动时间线
- 同时在 task run 卡片头部增加了当前活动摘要
- `workflow-canvas.tsx` 也会在节点卡片中展示最新一条活动消息，方便在画布上直接看每个节点正在做什么

### 65. 测试补充
- 更新 `backend/tests/services/test_agent_execution_service.py`
- 补充验证：
  - 执行成功后 `input_payload` 中会包含活动日志
  - `output_payload` 中也会保留活动日志
  - 原有执行链逻辑仍然通过

### 66. 验证补充
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_agent_execution_service tests.services.test_project_planning_service tests.services.test_workflow_run_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/services/agent_execution_service.py`
- `backend/tests/services/test_agent_execution_service.py`

## 本次追加

### 91. 修复 workflow run 对 ready task 的串行推进问题
- 调整 `workflow_run_service.py`
- 原来每次执行 workflow run 时，只会启动第一个 `ready` 的 task run，成功后立刻返回
- 现在改为：
  - 先按优先级排序 ready task run
  - 再选择一组“assigned agent 不冲突”的 ready task
  - 同一轮里并行启动这些 task run
- 同一个 agent 仍然保持不并发，避免重复占用

### 92. 新增 workflow 并行选择与 worker 执行辅助逻辑
- 新增 `_select_parallel_ready_task_runs(...)`
- 新增 `_execute_task_run_in_worker(...)`
- 每个并发 task run 使用独立数据库 session 执行，避免共用 session 带来的线程安全问题

### 93. 前端同步优化 workflow 执行中的乐观状态
- 更新 `execute-workflow-run-button.tsx`
- 执行整条 workflow 时，不再只把第一个 ready 节点标成 `in_progress`
- 现在会把当前一轮所有 ready 节点都先标记为执行中，和新的并行调度更一致

### 94. 测试补充
- 更新 `backend/tests/services/test_workflow_run_service.py`
- 新增/调整覆盖：
  - ready task 并行选择时会保留不同 agent 的任务
  - 同一个 agent 的多个 ready task 不会在同一轮同时启动

### 95. 验证补充
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_workflow_run_service tests.services.test_agent_execution_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/services/workflow_run_service.py`
- `backend/tests/services/test_workflow_run_service.py`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/execute-workflow-run-button.tsx`

## 本次追加

### 87. 取消基于 artifacts 的“不完整结果”检测
- 删除 `agent_execution_service.py` 中“没有产出 artifacts 就判定任务不完整”的逻辑
- 不再因为没有写文件 / 没有 artifacts 而自动重跑 task run
- artifacts 现在仅作为执行结果记录保留，不再参与完成性判断

### 88. 清理不再使用的 artifacts 输出要求辅助逻辑
- 移除仅用于“必须产出 artifacts”判定的辅助函数
- 保留原有 deferral 文案检测，只有明显推脱、要求补充输入、无法继续时才会继续判定为不完整

### 89. 测试补充
- 更新 `backend/tests/services/test_agent_execution_service.py`
- 新增覆盖：
  - 没有 artifacts 但结果文本正常时，不会再被判为不完整

### 90. 验证补充
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_agent_execution_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/services/agent_execution_service.py`
- `backend/tests/services/test_agent_execution_service.py`

## 本次追加

### 83. 取消 completed 对 `files` 数组的硬性要求
- 放松 `agent_execution_service.py` 中的最终响应校验
- 现在 agent 最终结果不再必须包含 `files`
- 只要最终响应不是工具请求、不是 `tool_calls` 中间态，且结构合法，就可以作为完成结果
- 若返回了 `files`，仍要求它必须是数组

### 84. 同步调整 task prompt 的最终输出说明
- 不再要求“无文件变更时也必须返回 `files: []`”
- 现在无文件输出时可以只返回：
  - `{"summary":"short result"}`
- 有文件输出时仍沿用原来的 `summary + files[]` 结构

### 85. 测试补充
- 更新 `backend/tests/services/test_agent_execution_service.py`
- 新增覆盖：
  - `summary` 无 `files` 时也能通过最终结果校验

### 86. 验证补充
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_agent_execution_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/services/agent_execution_service.py`
- `backend/tests/services/test_agent_execution_service.py`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/page.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/execute-task-run-button.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/execute-workflow-run-button.tsx`

## 本次追加

### 67. 修复画布仍没有上下滚动条的问题
- 之前 workflow 画布容器只设置了 `minHeight`
- 这会导致容器随着内容继续撑高，而不是进入纵向滚动
- 现在将画布视口改为固定可视高度：
  - `clamp(560px, 78vh, 960px)`
- 这样当节点内容高度超过视口时，就会稳定出现上下滚动条

### 68. 收紧自动布局的纵向间距
- 根据你的反馈，之前为了给连线留通道，把上下层节点间隔拉得过大
- 现在重新调整了布局参数：
  - `VERTICAL_GAP` 从较大的间距收紧到更适中的值
  - `ROW_LANE_MULTIPLIER` 也下调
- 结果是：
  - 节点不再挤在一条线上
  - 但上下层之间也不会再隔得过远

### 69. 同步更新布局测试预期
- 更新 `backend/tests/services/test_project_planning_service.py`
- 让自动布局断言匹配新的纵向间距和行位规则

### 70. 验证补充
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_project_planning_service tests.services.test_agent_execution_service tests.services.test_workflow_run_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`

## 本次追加

### 78. 将 agent task run 联网能力升级为 provider 原生 tool/function calling
- 不再只依赖模型手写 `tool_calls` JSON
- 为支持工具调用的模型优先启用 provider 原生工具协议
- 当前已接入：
  - OpenAI-compatible `tools/function calling`
  - Anthropic `tool_use / tool_result`
  - Google Gemini `functionCall / functionResponse`

### 79. 新增 provider 原生联网工具 schema 与参数归一化
- 在 `agent_execution_service.py` 中新增各 provider 的工具定义：
  - `web_search`
  - `fetch_url`
- 新增原生工具参数解析与归一化逻辑
- 后端统一将 provider 返回的结构化 tool call 转成现有工具执行格式，再复用现有 `web_search` / `fetch_url` 执行器

### 80. task run 执行链优先走原生 tools
- `task run` 的带活动日志执行链现在会优先走 `_execute_agent_roundtrip_native_tools(...)`
- 仅当模型不走原生 tools 时，旧的手写 JSON 工具协议才作为兼容逻辑保留在旧 roundtrip 中
- 同时调整 task prompt：
  - 支持原生 tools 的模型不再提示手写 `tool_calls` JSON
  - 改为明确要求直接调用提供的工具，且不要谎称工具已经执行

### 81. 测试补充
- 更新 `backend/tests/services/test_agent_execution_service.py`
- 新增覆盖：
  - 原生 tools 模式下的 prompt 文案
  - OpenAI-compatible 原生 function calling 往返执行

### 82. 验证补充
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_agent_execution_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/services/agent_execution_service.py`
- `backend/tests/services/test_agent_execution_service.py`

## 本次追加

### 76. 修正按下空格键本身会触发页面向下滚动的问题
- 在 `workflow-canvas.tsx` 中为 `Space` 快捷键补上 `preventDefault()`
- 现在按下空格时不会再触发浏览器默认的页面滚动
- 同时增加输入态判断，若焦点在 `input`、`textarea`、`select` 或可编辑区域，则不拦截空格输入

### 77. 验证补充
- 前端：
  - `npm.cmd run lint`
- 结果：
  - lint 通过

## 本次涉及文件补充
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`

## 本次追加

### 74. 修正按住空格滚轮缩放时滚动条仍然联动的问题
- 继续调整 `workflow-canvas.tsx` 的滚轮缩放逻辑
- 缩放时改为以鼠标当前位置作为缩放锚点，而不是只单纯修改 zoom
- 缩放后会立即校正 `scrollLeft / scrollTop`
- 同时增加 `onWheelCapture`，更早拦截滚轮默认滚动

### 75. 验证补充
- 前端：
  - `npm.cmd run lint`
- 结果：
  - lint 通过

## 本次涉及文件补充
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`
- `backend/app/services/project_planning_service.py`
- `backend/tests/services/test_project_planning_service.py`

## 本次追加

### 53. 优化 workflow 画布滚动体验，增加并统一上下左右滚动能力
- 将 workflow 画布的平移方式从“内部 transform 偏移”调整为“直接拖动滚动容器”
- 现在画布区域会稳定显示上下左右两个方向的滚动条
- 按住空格拖动画布时，本质上就是在同步控制 `scrollLeft / scrollTop`
- 因此拖动画布和滚动条的行为现在是一致的，不会再出现视觉位置与滚动位置不对齐的问题

### 54. 增加画布边缘留白
- 在 `workflow-canvas.tsx` 中新增 `CANVAS_PADDING`
- 自动给画布右侧和下侧留出更多空间
- 避免节点贴边、拖拽受限，滚动也更自然

### 55. 优化 AI 生成 workflow 的自动布局，尽量减少节点遮挡连线
- 调整 `project_planning_service` 中的布局参数：
  - 加大横向层间距
  - 加大纵向行间距
  - 为行与行之间增加“通道行”
- 现在分支节点之间的留白更大，合流节点也更容易落在分支中线附近
- 这样生成出来的 workflow 更规整，也更不容易出现节点压住连线、线条过于拥挤的问题

### 56. 测试补充
- 更新 `backend/tests/services/test_project_planning_service.py`
- 同步调整自动布局断言，覆盖新的分支 / 合流行位预期

### 57. 验证补充
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_project_planning_service tests.services.test_agent_execution_service tests.services.test_workflow_run_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`
- `backend/app/services/project_planning_service.py`
- `backend/tests/services/test_project_planning_service.py`

## 本次追加

### 58. 重新规划 AI 生成 workflow 的节点排布，避免所有任务落在一条线上
- 根据你的反馈，继续调整 `project_planning_service` 的自动布局策略
- 现在对于“单链路一层只有一个节点”的情况，不再默认始终保持同一水平线
- 会按依赖深度做交错式（zigzag）排布
- 这样可以减少长链 workflow 中所有节点横向一字排开的情况，也能给多依赖连线留出更多可读空间

### 59. 合流节点继续按父节点中线对齐
- 对多前置依赖的合流节点，布局不会简单跟随其中一个父节点
- 而是会继续尝试按父节点所在行的中线落位
- 同时增加“按通道取最近车道”的处理，避免因为取整方式导致合流节点偏到错误行

### 60. 测试补充
- 更新 `backend/tests/services/test_project_planning_service.py`
- 新增覆盖：
  - 单链路任务会交错排布，不再始终共线
  - 分支合流节点仍能落在更自然的中线附近
- 同步更新自动布局坐标断言，匹配新的布局预期

### 61. 验证补充
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_project_planning_service tests.services.test_agent_execution_service tests.services.test_workflow_run_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/services/project_planning_service.py`
- `backend/tests/services/test_project_planning_service.py`

## 本次追加

### 48. 为 AI 生成 workflow 的 task 增加建议输出文件规划
- 继续增强 `project_planning_service` 的 planner 输出结构
- 现在每个 task 还可以规划：
  - `suggested_output_paths`
- 用来表达该 task 更适合把结果落到 workspace 中哪些相对路径

### 49. 标准化 task description 进一步增强
- 生成后的 task description 现在除了：
  - `Objective`
  - `Deliverable`
  - `Required workspace files`
  - `Required upstream task outputs`
- 还会额外写入：
  - `Suggested output files`
- 这样后续 agent 执行 task 时，不只是知道要交付什么，还会更明确知道应该把结果落到哪里

### 50. Planner prompt 同步升级
- planner prompt 的 JSON 结构说明里新增了 `suggested_output_paths`
- 也明确告诉模型：
  - 如果任务应当创建文件，就给出具体的 workspace 相对输出路径
- 这样生成结果会更贴近实际工作流里的文件产出习惯

### 51. 测试补充
- 更新 `backend/tests/services/test_project_planning_service.py`
- 新增覆盖：
  - 标准化 description 会包含 `Suggested output files`
  - planner prompt 会包含 `suggested_output_paths` 结构说明

### 52. 验证补充
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_project_planning_service tests.services.test_agent_execution_service tests.services.test_workflow_run_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/services/project_planning_service.py`
- `backend/tests/services/test_project_planning_service.py`

## 本次追加

### 6. 修复删除 Task 触发整页 Runtime Error 的问题
- 当某个 task 仍然被其它任务依赖时，后端会返回：
  - `Task is still required by tasks: [...]`
- 之前前端删除表单直接调用 `deleteTaskAction`
- 这样当删除失败时，异常会直接冒泡成页面级 Runtime Error

### 7. 删除 Task 改为走表单反馈流
- 新增 `submitDeleteTaskAction`
- 删除操作现在和“编辑 task / 更新状态”一样，改为通过 `useActionState` 处理结果
- 删除失败时会在前端弹出错误提示，不再把整个页面打成运行时异常
- 删除按钮也补上了 pending 态，避免重复点击

### 8. 验证
- 前端：
  - `npm.cmd run lint`
- 结果：
  - lint 通过

## 本次涉及文件补充
- `frontend/lib/actions.ts`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/task-row-actions-client.tsx`

## 本次追加

### 9. 为 Agent 增加受控联网能力
- 在 `backend/app/services/agent_execution_service.py` 中新增了两类后端工具：
  - `web_search`：用于按关键词搜索网页结果
  - `fetch_url`：用于抓取指定网页正文文本
- 这两个工具不是让模型直接自由联网，而是由服务端按受控协议代执行，再把结果回填给模型继续完成任务

### 10. Agent 执行链支持多轮“请求工具 -> 回填结果 -> 最终输出”
- 扩展了 agent 执行协议：模型现在可以先返回 `tool_calls`
- 后端会解析 `tool_calls`、执行工具、整理结果，并再次把结果交给模型
- 在达到最终回答后，仍然沿用现有的 `summary + files` 输出协议，不影响原来的文件写入逻辑

### 11. 增加联网能力的安全边界与配置项
- 在 `backend/app/core/config.py` 中新增联网相关配置：
  - `BACKEND_AGENT_WEB_ENABLED`
  - `BACKEND_AGENT_WEB_MAX_ITERATIONS`
  - `BACKEND_AGENT_WEB_SEARCH_MAX_RESULTS`
  - `BACKEND_AGENT_WEB_FETCH_MAX_CHARS`
- 默认限制了最大搜索结果数、单页正文长度和工具调用轮数，避免 agent 无限请求联网工具
- `fetch_url` 仅允许 `http` / `https` 链接

### 12. 执行结果中记录联网工具结果
- `task_run.output_payload` 新增 `tool_results`
- 这样后续在前端或排障时，可以直接看到 agent 这次执行过程中调用了哪些联网工具，以及返回了什么结果

### 13. 测试补充
- 更新 `backend/tests/services/test_agent_execution_service.py`
- 新增覆盖：
  - agent 多轮工具调用执行成功
  - `tool_calls` 解析
  - `web_search` 搜索结果解析
  - `fetch_url` 内容截断

### 14. 验证
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_agent_execution_service tests.services.test_workflow_run_service tests.services.test_project_planning_service`
- 结果：
  - 单测通过

## 本次涉及文件补充
- `backend/app/core/config.py`
- `backend/app/services/agent_execution_service.py`
- `backend/tests/services/test_agent_execution_service.py`

## 本次追加

### 42. 优化 AI 生成 workflow 的任务标准化能力
- 升级了 `project_planning_service` 的 planner 输出结构
- 现在 planner 不再只生成简单的 `title / description / depends_on_titles`
- 还可以生成：
  - `objective`
  - `deliverable`
  - `input_from_titles`
  - `required_workspace_paths`
- 后端会把这些结构化字段统一组装为更标准的 task description

### 43. 让生成的 task 更贴近真实 workspace 工作环境
- planner prompt 现在会带上：
  - `workspace root path`
  - workspace 内采样到的一批现有文件路径
  - 现有 project task 列表
  - 可用 agent 信息
- 这样模型生成 task 时会更容易围绕真实文件环境去规划，而不是只产出泛化的“分析 / 实现 / 测试”模板任务

### 44. 提升 workflow 依赖关系准确性
- planner 现在可以区分：
  - `depends_on_titles`：阻塞型前置依赖
  - `input_from_titles`：需要引用其输出或信息的上游任务
- 后端会将两者合并为实际 task dependencies
- 这样一来，如果某个任务需要多个前置任务的文件或信息，生成的 workflow 也能更自然地形成多依赖结构，而不是被误压成单链路

### 45. 提升生成 task 对执行链的可用性
- 标准化后的 task description 会自动写入执行约束，例如：
  - `Objective`
  - `Deliverable`
  - `Required workspace files`
  - `Required upstream task outputs`
  - 必须直接完成任务而不是停留在解释层
- 这样后续 agent 执行 task 时，更容易读到明确输入、输出和上下文来源

### 46. 测试补充
- 更新 `backend/tests/services/test_project_planning_service.py`
- 新增覆盖：
  - `input_from_titles` 会合并进真实依赖
  - 标准化 description 会包含 workspace 文件和交付约束
  - planner prompt 会包含 workspace 文件上下文和新增结构字段说明

### 47. 验证补充
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_project_planning_service tests.services.test_agent_execution_service tests.services.test_workflow_run_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/services/project_planning_service.py`
- `backend/tests/services/test_project_planning_service.py`

## 本次追加

### 31. Task 执行遇到“答非所问 / 无实际产出”时先自动重跑，不再直接 failed
- 根据你的反馈，调整了 agent task 的失败策略
- 现在当 task 最终输出虽然格式合法，但业务上明显没有完成时，不会立刻失败
- 会先在同一次 task run 内自动重试若干次，再决定是否失败

### 32. 新增“不完整结果”检测规则
- 在 `backend/app/services/agent_execution_service.py` 中新增：
  - `_task_requires_artifact_output`
  - `_detect_incomplete_task_result`
- 当前会识别这类结果为“不完整”：
  - 数据采集 / 整理 / 导出 / 文件生成类 task 没有产出 artifact
  - 输出里出现明显的推迟执行 / 要求补充输入 / 无法继续的表述
- 遇到这种情况会自动重跑，并在 retry prompt 里明确要求 agent 直接完成任务、产出真实文件

### 33. 新增自动重跑配置
- 在 `backend/app/core/config.py` 中新增：
  - `BACKEND_AGENT_TASK_AUTO_RETRY_ATTEMPTS`
- 默认自动重跑 3 次
- `task_run.input_payload` 里也会记录当前是第几次自动尝试，便于排查

### 34. 画布执行中状态补为 in_progress
- 为了解决 workflow 画布上只看到 `todo / done`、看不到执行中的问题
- 在 `workflow-canvas.tsx` 中新增了前端乐观运行态：
  - 点击执行单个 task run 时，会立即把对应节点临时标成 `in_progress`
  - 点击执行整个 workflow 时，也会在每一步开始前把当前将执行的 task 节点临时标成 `in_progress`
- 对应事件由：
  - `execute-task-run-button.tsx`
  - `execute-workflow-run-button.tsx`
 通过浏览器自定义事件通知画布

### 35. 测试补充
- 更新 `backend/tests/services/test_agent_execution_service.py`
- 新增覆盖：
  - 不完整结果会触发自动重跑
  - 自动重跑后成功产出 artifact 时，task run 正常完成
  - 自动重跑相关字段会写入 `input_payload`

### 36. 验证补充
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_agent_execution_service tests.services.test_workflow_run_service tests.services.test_project_planning_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/core/config.py`
- `backend/app/services/agent_execution_service.py`
- `backend/tests/services/test_agent_execution_service.py`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/execute-task-run-button.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/execute-workflow-run-button.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/page.tsx`

## 本次追加

### 37. 修复 Agent `web_search` 能联网但经常返回空结果的问题
- 实测确认后端进程本身可以访问外网，但 `agent_execution_service._execute_web_search(...)` 经常返回空数组
- 排查后发现问题不在“不能联网”，而在 DuckDuckGo 返回的页面结构与当前解析规则不一致
- 原先的实现只匹配 `class="result__a"` 这一种结果链接结构，因此遇到 DuckDuckGo 的其他页面变体时会直接解析不到任何结果

### 38. 调整搜索抓取请求头并增强结果解析兼容性
- 将搜索页抓取的 `User-Agent` 改为更接近正常浏览器的 UA
- 新增 `Accept-Language`
- `web_search` 现在同时支持：
  - 标准 `result__a` 结果链接
  - 回退匹配 DuckDuckGo 重定向链接的通用解析路径
- 对 `//` 协议相对地址和 `uddg=` 重定向链接也做了统一还原

### 39. 联网复测
- 直接在后端环境中复测 `_execute_web_search("OpenAI latest API docs", max_results=3)`
- 已能稳定返回真实搜索结果，例如：
  - `OpenAI API Platform Documentation`
  - `OpenAPI specification for the OpenAI API - GitHub`
- 说明当前 `web_search` 的核心空结果问题已修复

### 40. 测试补充
- 更新 `backend/tests/services/test_agent_execution_service.py`
- 新增覆盖：
  - 标准 `result__a` 解析
  - DuckDuckGo 重定向链接的回退解析

### 41. 验证补充
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_agent_execution_service tests.services.test_workflow_run_service tests.services.test_project_planning_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/services/agent_execution_service.py`
- `backend/tests/services/test_agent_execution_service.py`

## 本次追加

### 26. 修复 Agent 直接返回工具请求对象却被标记为 completed 的问题
- 发现有些模型响应不是 `{"summary":"...","files":[]}` 这类最终结果
- 而是直接返回单个工具请求对象，例如：
  - `{"tool":"web_search","query":"...","max_results":5}`
- 这种内容本质上仍然是“中间态”，之前却会被当成最终输出写入 `task_run.output_payload`
- 从而导致 task run 被错误标记为 `completed`，workflow 继续向下推进

### 27. 增加最终输出严格校验
- 在 `backend/app/services/agent_execution_service.py` 中新增 `_validate_final_response_payload`
- 现在最终输出必须满足：
  - 不能是单个 `tool` 请求对象
  - 不能仍然包含 `tool_calls`
  - 必须带合法的 `files` 数组
  - 如果有 `summary`，必须是字符串
- 不满足时直接按执行失败处理，而不是继续当作成功结果

### 28. 修复最终输出校验失败时状态没有落成 failed 的问题
- 原先 `_apply_file_operations` 内部抛出的校验异常，没有统一回写 task run / workflow run 失败状态
- 现已补上异常兜底
- 所以像“只返回 `web_search` 请求”的情况，现在会正确变成：
  - task run = `failed`
  - workflow run = `failed`
- 不会再显示成 completed

### 29. 测试补充
- 更新 `backend/tests/services/test_agent_execution_service.py`
- 新增覆盖：
  - 单个工具请求对象可被识别为工具请求
  - 最终输出校验会拒绝单个 `tool` 请求对象
  - 最终输出校验会拒绝带 `tool_calls` 的伪最终结果
  - `execute_task_run` 遇到这种情况时会正确写回 `failed`

### 30. 验证补充
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_agent_execution_service tests.services.test_workflow_run_service tests.services.test_project_planning_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/services/agent_execution_service.py`
- `backend/tests/services/test_agent_execution_service.py`

## 本次追加

### 15. 修复 Workflow Run 执行时页面只在最后一次性变更状态的问题
- 后端 `execute_workflow_run` 改为“每次调用只推进一个 ready task run”
- 不再由一次请求同步跑完整条 workflow
- 这样前端可以在每推进一步之后刷新页面，逐步看到每个 task run 的状态变化

### 16. 前端执行 Workflow 改为循环推进并逐步刷新
- 重写 `ExecuteWorkflowRunButton`
- 点击“执行整个 Workflow”后，前端会持续调用执行接口，并在每一步后 `router.refresh()`
- 现在 workflow 运行过程中，画布节点状态和下方 `Run Detail` 会一起跟着 task run 进度刷新，而不是等到最后一起变成 done

### 17. 单个 Task Run 执行后立即刷新画面
- 重写 `ExecuteTaskRunButton`
- 执行单个 task run 成功后会立刻刷新当前页面
- 避免 Run Detail 已更新但画布仍停留在旧状态，或反过来的不同步现象

### 18. 修复并优化画布连线预览效果
- 画布进入连线模式后，移动到目标节点上时，预览线现在会吸附到目标节点左侧边缘
- 新增单独的预览箭头样式，避免预览线和正式连线混在一起
- 点击空白区域时会取消当前连线，减少误操作
- 目标节点悬停时会有高亮，连线过程更清晰

### 19. Workflow 页顶部执行入口同步改为新机制
- 重写 `workflow/page.tsx`
- Run Detail 顶部的“执行整个 Workflow”入口也改为使用新的前端循环推进按钮
- 避免顶部入口和画布内部按钮行为不一致

### 20. 测试补充
- 更新 `backend/tests/services/test_workflow_run_service.py`
- 新增覆盖：
  - `execute_workflow_run` 每次只推进一个 ready task run
  - 原有 busy agent 时转为 queued 的行为仍然正确

### 21. 验证
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_workflow_run_service tests.services.test_agent_execution_service tests.services.test_project_planning_service`
- 前端：
  - `npm.cmd run lint`
- 结果：
  - 单测通过
  - lint 通过

## 本次涉及文件补充
- `backend/app/services/workflow_run_service.py`
- `backend/tests/services/test_workflow_run_service.py`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/page.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/execute-workflow-run-button.tsx`
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/execute-task-run-button.tsx`

## 本次追加

### 22. 修复 Agent 联网后中间 JSON 被误当成最终输出的问题
- 发现当模型在联网多轮执行后，把“上一轮带 `tool_calls` 的 JSON”和“本轮最终 JSON”连续输出时
- 原来的解析只会尝试把整段文本当成一个 JSON 解析
- 一旦失败，就会把整段原样当成普通文本返回，导致：
  - task 被错误标记为 `completed`
  - Output 区域直接出现 `tool_calls` JSON 片段
  - downstream task 会把这个半成品结果当成已完成前置继续推进

### 23. 调整 Agent 响应解析策略
- 在 `backend/app/services/agent_execution_service.py` 中新增多 JSON 提取逻辑
- 现在会从模型响应里提取多个 JSON 对象，并优先采用最后一个 JSON 作为最终结果
- 对于联网 roundtrip，在识别到最终阶段没有 `tool_calls` 时，会将最后一个 JSON 规范化后再返回给落库层
- 这样即使模型把“上一轮 JSON + 最终 JSON”一起吐回来，也不会再把中间工具请求误显示为最终输出

### 24. 测试补充
- 更新 `backend/tests/services/test_agent_execution_service.py`
- 新增覆盖：
  - 连续多个 JSON 时优先取最后一个
  - 联网 roundtrip 遇到“前一个工具请求 JSON + 后一个最终 JSON”时，最终仍能正确落到最终结果

### 25. 验证补充
- 后端：
  - `.\venv\Scripts\python.exe -m unittest tests.services.test_agent_execution_service tests.services.test_workflow_run_service tests.services.test_project_planning_service`
- 结果：
  - 单测通过

## 本次涉及文件补充
- `backend/app/services/agent_execution_service.py`
- `backend/tests/services/test_agent_execution_service.py`
## 本次追加

### 71. 调整画布缩放手势，改为按住空格再滚轮才缩放
- 修改 `workflow-canvas.tsx` 中的 `handleWheel`
- 现在只有在 `spacePressed = true` 时才会拦截滚轮并调整 zoom
- 未按空格时，滚轮恢复为正常滚动画布，不再误触发缩放

### 72. 同步更新画布顶部交互提示
- 将提示文案从“鼠标在画布内可直接滚轮缩放”改为“按住空格再滚轮可缩放画布”
- 让页面说明与实际交互保持一致

### 73. 验证补充
- 前端：
  - `npm.cmd run lint`
- 结果：
  - lint 通过

## 本次涉及文件补充
- `frontend/app/workspaces/[workspaceId]/projects/[projectId]/workflow/workflow-canvas.tsx`
